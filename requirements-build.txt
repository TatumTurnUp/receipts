# Only needed to build a distributable app; not needed to run from source.
-r requirements.txt
pyinstaller>=6.6
# cairosvg is only needed to regenerate icons from icon.svg:
#     pip install cairosvg && python build-assets/make_icons.py
